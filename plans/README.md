# Implementation Plans

Generated by the improve skill on 2026-07-19 (audit at commit `fba657a`). Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
| --- | --- | --- | --- | --- | --- |
| 001 | Make typecheck and lint pass (verification baseline) | P1 | M | — | DONE |
| 002 | Fix off-by-one blog dates + modifier-click hijack | P1 | S | 001 | BLOCKED (Vitest hits the environment file-watch limit) |
| 003 | Add SEO and social-sharing metadata | P2 | M | 002 | DONE |
| 004 | Wire up pre-commit hook, clean tracked artifacts | P2 | S | 001 | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 002, 003, 004 all require 001: it is the only thing that makes `bun run typecheck` / `bun run lint` usable as verification gates.
- 003 lands after 002 because both edit `src/routes/blog/$slug.tsx` and `src/routes/index.tsx`; 002 also adds the `formatPostDate` helper that 003's post page continues to use.
- 004 is independent of 002/003 and can run in parallel with them (different files), but its hook-canary check is only meaningful once 001 makes staged checks pass.

## Findings considered and rejected (do not re-audit)

- **`bun audit` advisories (1 critical / 7 high)**: all confined to dev-only chains (`@tanstack/devtools-vite → shell-quote`, `jsdom`/`shadcn` CLI → `undici`, vitest → `ws`); the hono runtime advisories (JWT/CORS middleware) are unreachable for a static personal site with no auth or user input. Routine `bun update` at the owner's leisure; not worth a plan.
- **`TanStackDevtools` rendered unconditionally in `__root.tsx`**: by-design — TanStack devtools strip themselves from production builds.
- **MDX paragraph backtick re-processing (`mdx-components.tsx:42-70`) likely dead code** (MED confidence): MDX converts inline backticks before the `p` component sees strings. Reported to owner; not selected this round. Plan 001 suppresses (not fixes) the related `no-base-to-string` lint error at `mdx-components.tsx:27`.
- **Stale welcome-post content** (`welcome-to-my-blog.mdx` claims the site is "built using Next.js" and promises a contact form): content decision left to the owner.
- **79 lint errors in `src/components/ui/**`\*\*: owner's standing instruction is not to fix vendored shadcn lint issues; plan 001 excludes the directory from lint instead.

## Direction items surfaced but not planned (owner's call)

- Contact form (welcome post promises one; commit `4744011` had a working one before the v7 rewrite; nitro server available).
- RSS feed + sitemap (pairs with plan 003's frontmatter descriptions).
- Mobile drawer nav is empty (`__root.tsx:175` renders `<nav />`); populate or remove.
